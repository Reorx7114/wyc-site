"use client";
import {useEffect,useState} from "react";import{serviceRequestStatuses,type ServiceRequest,type ServiceRequestStatus}from"@/lib/service-requests";type StatusFilter="全部"|ServiceRequestStatus;
export function AdminServiceRequests({password}:{password:string}){const[requests,setRequests]=useState<ServiceRequest[]>([]),[message,setMessage]=useState("案件載入中..."),[loading,setLoading]=useState(true),[filter,setFilter]=useState<StatusFilter>("全部"),[expandedIds,setExpandedIds]=useState<string[]>([]);useEffect(()=>{loadRequests()},[password]);async function loadRequests(){setLoading(true);const response=await fetch("/api/admin/service-requests",{cache:"no-store",headers:{"x-admin-password":password}});const result=await response.json();setRequests(result.requests??[]);setMessage(result.ok?"":result.message||"案件載入失敗。");setLoading(false)}async function save(request:ServiceRequest){setMessage("案件更新中...");const response=await fetch("/api/admin/service-requests",{method:"PATCH",headers:{"Content-Type":"application/json","x-admin-password":password},body:JSON.stringify({id:request.id,status:request.status,adminNotes:request.adminNotes,publicReply:request.publicReply})});const result=await response.json();setMessage(result.ok?"案件已更新。":result.message||"案件更新失敗。");if(result.ok)await loadRequests()}async function remove(request:ServiceRequest){if(!window.confirm(`確定刪除案件 ${request.publicReference} 嗎？相關照片也會一起刪除。`))return;const response=await fetch("/api/admin/service-requests",{method:"DELETE",headers:{"Content-Type":"application/json","x-admin-password":password},body:JSON.stringify({id:request.id})});const result=await response.json();setMessage(result.ok?"案件已刪除。":result.message||"案件刪除失敗。");if(result.ok)await loadRequests()}function updateLocal(id:string,patch:Partial<ServiceRequest>){setRequests(current=>current.map(request=>request.id===id?{...request,...patch}:request))}function toggle(id:string){setExpandedIds(current=>current.includes(id)?current.filter(item=>item!==id):[...current,id])}const filters:StatusFilter[]=["全部",...serviceRequestStatuses],visible=filter==="全部"?requests:requests.filter(request=>request.status===filter),count=(status:StatusFilter)=>status==="全部"?requests.length:requests.filter(request=>request.status===status).length;return <div><div className="mb-6 flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-2xl font-black text-forest">案件管理</h2><p className="mt-2 text-forest/60">僅供後台檢視。姓名、電話、地址與照片不會顯示在前台。</p></div><button onClick={loadRequests} className="rounded-full border border-forest/15 bg-white px-5 py-3 font-bold text-forest">重新整理</button></div><div className="mb-6 flex flex-wrap gap-2">{filters.map(status=><button key={status} onClick={()=>setFilter(status)} className={`rounded-full px-4 py-2 text-sm font-bold ${filter===status?"bg-forest text-white":"border border-forest/10 bg-white text-forest"}`}>{status}（{count(status)}）</button>)}</div>{message&&<p className="mb-6 rounded-2xl bg-white p-4 font-bold text-forest shadow-soft">{message}</p>}{!loading&&!visible.length&&<p className="rounded-2xl bg-white p-6 text-forest/60 shadow-soft">目前沒有符合條件的案件。</p>}<div className="space-y-4">{visible.map(request=>{const expanded=expandedIds.includes(request.id);return <article key={request.id} className="rounded-[2rem] bg-white p-5 shadow-soft md:p-6"><div className="grid items-center gap-4 md:grid-cols-[minmax(0,1fr)_auto_auto]"><div><div className="flex flex-wrap items-center gap-2"><b className="text-xl text-forest">{request.name}</b><span className="rounded-full bg-blush/60 px-3 py-1 text-sm font-bold text-forest">{request.category}</span><span className="rounded-full bg-cream px-3 py-1 text-sm font-bold text-forest">{request.status}</span>{request.isTest&&<span className="rounded-full bg-amber-100 px-3 py-1 text-sm font-bold text-amber-800">測試資料</span>}</div><p className="mt-2 text-sm text-forest/55">案件編號：{request.publicReference} ・ {new Date(request.createdAt).toLocaleString("zh-TW")}</p></div><select value={request.status} onChange={event=>updateLocal(request.id,{status:event.target.value as ServiceRequestStatus})} className="rounded-full border border-forest/10 bg-cream px-4 py-3 font-bold text-forest">{serviceRequestStatuses.map(status=><option key={status}>{status}</option>)}</select><button onClick={()=>toggle(request.id)} className="rounded-full border border-forest/15 px-5 py-3 font-bold text-forest">{expanded?"收合詳情":"查看詳情"}</button></div>{expanded&&<div className="mt-5 border-t border-forest/10 pt-5"><div className="grid gap-4 rounded-2xl bg-cream p-5 md:grid-cols-2"><p><b>電話：</b><a className="text-rose underline" href={`tel:${request.phone}`}>{request.phone}</a></p><p><b>地址：</b>{request.address||"未提供"}</p></div><p className="mt-5 whitespace-pre-wrap leading-8 text-forest/75">{request.content}</p>{(request.imageUrls?.filter(Boolean).length??0)>0&&<div className="mt-5 grid grid-cols-2 gap-3 md:grid-cols-4">{(request.imageUrls??[]).filter(Boolean).map((url,index)=><a key={`${url}-${index}`} href={url} target="_blank" rel="noreferrer"><img src={url} alt={`案件照片 ${index+1}`} className="h-36 w-full rounded-2xl object-cover"/></a>)}</div>}<label className="mt-5 block"><span className="font-bold text-forest/70">公開回覆</span><textarea value={request.publicReply} onChange={event=>updateLocal(request.id,{publicReply:event.target.value})} rows={3} placeholder="里民查詢案件進度時會看到這段內容。" className="mt-2 w-full rounded-2xl border border-forest/10 bg-cream px-4 py-3"/></label><label className="mt-5 block"><span className="font-bold text-forest/70">內部備註</span><textarea value={request.adminNotes} onChange={event=>updateLocal(request.id,{adminNotes:event.target.value})} rows={3} className="mt-2 w-full rounded-2xl border border-forest/10 bg-cream px-4 py-3"/></label><div className="mt-5 flex flex-wrap gap-3"><button onClick={()=>save(request)} className="rounded-full bg-forest px-6 py-3 font-bold text-white">儲存案件</button><button onClick={()=>remove(request)} className="rounded-full border border-rose/30 px-6 py-3 font-bold text-rose">刪除案件</button></div></div>}</article>})}</div></div>}
